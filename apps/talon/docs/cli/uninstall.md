---
summary: "CLI reference for `talon uninstall` (remove gateway service + local data)"
read_when:
  - You want to remove the gateway service and/or local state
  - You want a dry-run first
title: "uninstall"
---

# `talon uninstall`

Uninstall the gateway service + local data (CLI remains).

```bash
talon uninstall
talon uninstall --all --yes
talon uninstall --dry-run
```
